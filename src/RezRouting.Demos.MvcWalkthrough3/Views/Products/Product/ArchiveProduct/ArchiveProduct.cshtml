@using RezRouting.Demos.MvcWalkthrough3.Controllers.Products.Product
@model TaskModel<ArchiveProductRequest>

@{
    ViewBag.Title = "Archive Product";
}

<form method="POST" action="@Request.Url.LocalPath">

    @Html.ValidationSummary()

    @Html.HiddenFor(x => x.Request.Id)
    <div class="form-group">
        @Html.LabelFor(x => x.Request.ArchiveComments, "Comments")
        @Html.TextAreaFor(x => x.Request.ArchiveComments, new { @class = "form-control" })
    </div>

    <input class="btn btn-primary" type="submit" value="Archive" />

</form>
