namespace RezRouting.Resources
{
    /// <summary>
    /// Defines the function / method that handles an individual route. The RezRouting 
    /// core configuration is framework-neutral and concrete implementations of IRouteHandler 
    /// exist for each framework.
    /// </summary>
    public interface IRouteHandler
    {
        
    }
}